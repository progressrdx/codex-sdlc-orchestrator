# Final user-journey verification

## Source under test

- Git commit:
- Source tree fingerprint:
- Build command and result:
- Test command and result:

## Required checks

Record each check as `pass` or `not_applicable`. A `not_applicable` check must be justified in its own line below (for example a headless deliverable has no UI quality surface); the coordinator discloses every waived check to the user.

- `launch`: pass/not_applicable — actual launch path and evidence
- `core_outcomes`: pass/not_applicable — each confirmed goal exercised
- `content_semantics`: pass/not_applicable — values, labels, missing/unsupported states
- `interactions`: pass/not_applicable — controls and failure recovery
- `external_links`: pass/not_applicable — each destination actually opened
- `ui_quality`: pass/not_applicable — truncation, overflow, layout, accessibility
- `release_hygiene`: pass/not_applicable — no mock selectors, debug controls, placeholders
- `source_truth`: pass/not_applicable — displayed data verified against the intended source

## Defects and criterion verdicts

- Defects:
- `AC-*` verdicts:
- Regression reruns:

Screenshots and source inspection may supplement this report but cannot replace executed evidence.
