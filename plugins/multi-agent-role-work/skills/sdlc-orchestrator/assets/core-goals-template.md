# 用户确认的核心目标

## 用户确认

- 确认人：
- 确认依据：
- 确认日期：

## 核心结果

- `GOAL-001`：
- `GOAL-002`：

每个目标都要写清用户价值和可观察的成功结果。不要把演示、原型、框架或实现捷径写成最终目标。

## 范围完整性

- 不得静默缩减的内容：
- 已知非目标：
- 后续任何缩减都必须有用户单独批准的 `SC-*` 记录，并标明受影响的 `GOAL-*` 或 `AC-*`。
